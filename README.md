# پروژه مدیریت محصولات با جنگو و داکر

این پروژه یک وب اپلیکیشن ساده برای مدیریت محصولات است که با استفاده از فریمورک جنگو (Django) برای بک‌اند و PostgreSQL به عنوان دیتابیس ساخته شده است. کل پروژه به صورت کامل با استفاده از Docker و Docker Compose کانتینرسازی شده است تا راه‌اندازی و توسعه آن در هر محیطی به سادگی امکان‌پذیر باشد.

این پروژه تمام عملیات اصلی **CRUD (Create, Read, Update, Delete)** را از طریق پنل ادمین جنگو پوشش می‌دهد.

---

## 🚀 تکنولوژی‌های استفاده شده

- **Backend:** Django
- **Database:** PostgreSQL
- **Containerization:** Docker, Docker Compose
- **Server:** Django Development Server

---

## 📋 پیش‌نیازها

برای اجرای این پروژه، شما فقط به دو ابزار زیر نیاز دارید:

- [Docker](https://www.docker.com/products/docker-desktop)
- [Docker Compose](https://docs.docker.com/compose/install/) (معمولاً به همراه داکر نصب می‌شود)

---

## ⚙️ مراحل راه‌اندازی و اجرا

برای راه‌اندازی پروژه در سیستم خود، مراحل زیر را به ترتیب دنبال کنید:

**۱. کلون کردن ریپازیتوری (در صورت وجود):**
```bash
git clone <your-repository-url>
cd django_docker_project
```

**۲. ساخت فایل متغیرهای محیطی (`.env`):**
یک فایل با نام `.env` در ریشه اصلی پروژه بسازید و محتویات زیر را در آن قرار دهید. این فایل شامل اطلاعات حساس دیتابیس است.

```ini
POSTGRES_DB=product_db
POSTGRES_USER=product_user
POSTGRES_PASSWORD=securepassword
DB_HOST=db
```

**۳. ساخت و اجرای کانتینرها:**
دستور زیر را در ترمینال اجرا کنید. این دستور ایمیج‌های داکر را بر اساس `Dockerfile` می‌سازد، کانتینرها را اجرا می‌کند و شبکه مورد نیاز را ایجاد می‌نماید.

```bash
docker-compose up --build -d
```
*   فلگ `--build` باعث می‌شود ایمیج‌ها از نو ساخته شوند.
*   فلگ `-d` باعث می‌شود کانتینرها در پس‌زمینه اجرا شوند.

**۴. اجرای مایگریشن‌های دیتابیس:**
بعد از اینکه کانتینرها با موفقیت اجرا شدند، دستور زیر را برای ساختن جداول در دیتابیس PostgreSQL اجرا کنید:

```bash
docker-compose exec web python manage.py migrate
```

**۵. ساخت کاربر ادمین (Superuser):**
برای دسترسی به پنل مدیریت جنگو، یک کاربر ادمین بسازید:

```bash
docker-compose exec web python manage.py createsuperuser
```
(از شما خواسته می‌شود نام کاربری، ایمیل و رمز عبور را وارد کنید).

**تبریک!** پروژه اکنون آماده است.
- وب‌سایت در آدرس `http://localhost:8000` قابل مشاهده است.
- پنل ادمین در آدرس `http://localhost:8000/admin` در دسترس است.

---

## 🛠️ نحوه تست عملیات CRUD

برای تست کامل پروژه، وارد پنل ادمین شوید و مراحل زیر را انجام دهید:

1.  **Create:** در بخش "Products"، روی دکمه "+ Add" کلیک کرده و یک محصول جدید با نام و قیمت دلخواه بسازید.
2.  **Read:** لیست تمام محصولات ساخته شده را در صفحه "Products" مشاهده کنید.
3.  **Update:** روی نام یک محصول کلیک کنید تا وارد صفحه ویرایش آن شوید. اطلاعات آن را تغییر داده و ذخیره کنید.
4.  **Delete:** از لیست محصولات، یک یا چند محصول را با استفاده از چک‌باکس انتخاب کرده و از منوی "Action"، گزینه "Delete selected products" را اجرا کنید.

---

## 🐳 نحوه استفاده از داکر در پروژه

- **`Dockerfile`**: این فایل دستورالعمل گام به گام ساخت ایمیج برای اپلیکیشن جنگو است. این فایل شامل نسخه پایتون، نصب وابستگی‌ها از `requirements.txt` و دستور اجرای سرور جنگو می‌باشد.
- **`docker-compose.yml`**: این فایل ارکستریتور اصلی پروژه است. این فایل دو سرویس `web` (جنگو) و `db` (PostgreSQL) را تعریف، پیکربندی و به یکدیگر متصل می‌کند.
- **Volume**: یک والیوم به نام `postgres_data` برای سرویس `db` تعریف شده است تا اطلاعات دیتابیس با خاموش و روشن شدن کانتینرها از بین نرود و به صورت دائمی ذخیره شود.
